<div class={`fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center transition-opacity duration-300 ${isOpen ? 'opacity-100' : 'opacity-0 pointer-events-none'}`}>
  <div class="bg-white p-6 rounded-lg max-w-lg w-full">
    <button class="absolute top-2 right-2" onClick={closeModal}>Close</button>
    <h2 class="text-2xl font-bold">{selectedProject.title}</h2>
    <p class="mt-4">{selectedProject.description}</p>
  </div>
</div>
